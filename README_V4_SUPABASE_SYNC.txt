Projet Appartement V4 — Supabase Sync

Tables Supabase à créer dans le schema public :

1) apartment_settings
- id int8 primary key
- rate numeric
- savin_rate numeric
- vat_rate numeric

2) apartment_savings
- id int8 primary key generated by default as identity
- created_at timestamptz default now()
- month text
- date text
- planned numeric
- note text
- position int8

3) apartment_expenses
- id int8 primary key generated by default as identity
- created_at timestamptz default now()
- date text
- label text
- amount numeric
- currency text
- month text
- status text
- position int8

Important :
- Désactiver RLS sur ces 3 tables, ou ajouter des policies publiques comme pour Mes Comptes.
- Upload index.html, style.css et script.js sur GitHub Pages.
- Les premières données seront envoyées automatiquement dans Supabase si les tables sont vides.
